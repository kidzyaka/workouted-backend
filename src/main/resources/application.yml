spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workouted
    username: workouted_user
    password: workouted_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: aVeryLongSecretKeyForJwtAuthenticationThatMustBeAtLeast256BitsLongSoWeMakeItLonger
  expirationMs: 86400000 # 24 hours

server:
  port: 8080

app:
  admin:
    password: WrkOut_Admin_991!
